# v3 — UI screenshot baseline

Generated by `npm run screenshots` on 2026-08-01.

Viewport 1512×806 at DPR 2, so PNGs are
3024px wide. *(fullpage)* shots are taller than the viewport;
the sticky header appears mid-image in those, which is a capture artifact, not a layout bug.

**Dark theme only** — the app has no light mode (single hardcoded palette in
`public/style.css`, no `prefers-color-scheme` query, no theme toggle).

## How this was produced

A throwaway scheduler on its own port against a temp `CS_DATA`; `~/.claude-scheduler` is
never touched. `claudePath` is pre-seeded to a fake binary, so the usage meters read fixed
values (5h 37%, weekly 64%, Fable 90%) and **no real `claude` can run** — a capture spends
no API quota. Only `command`-type jobs are ever executed; the three `claude` jobs are
created disabled and never fired. No project is ever activated, which is why the burst
dialog shows its "No activated projects" state.

The Sessions tab is seeded separately via `CS_SESSIONS_ROOT` pointed at four planted
`.jsonl` transcripts (never `~/.claude/projects`): a parallel-tool-call session, an
Edit/TodoWrite/Grep session, a markdown-with-code-fence session, and a `cwdGuessed`
session. See `tools/screenshots/seed.mjs`'s `buildFixtureSessions`.

Run statuses present: fail, killed, ok, running, skipped, stopped, timeout. `queued` is unreachable without
firing a claude job past its concurrency cap.

## Contents

### Zero states

| File | State |
|---|---|
| `01-jobs-empty.png` | Jobs tab with no jobs yet — "Create your first job" |
| `02-projects-empty.png` | Projects tab with nothing registered and no projectRoots set |
| `03-settings-default.png` | Settings at first boot, before any demo values are written *(fullpage)* |

### Populated UI

| File | State |
|---|---|
| `04-jobs-populated.png` | Jobs list: mixed statuses, usage banner, running badge |
| `05-jobs-populated-fullpage.png` | Every job row including the three claude jobs *(fullpage)* |
| `06-jobs-live-run.png` | A row mid-run: pulsing "running" with wind-down + stop-now controls |
| `07-jobs-filter-match.png` | Filter box narrowing the list |
| `08-jobs-filter-no-match.png` | "No jobs match your filter." |
| `09-history-all.png` | Run history across every seeded status, incl. a budget-guard skip reason |
| `10-history-fullpage.png` | The whole run list *(fullpage)* |
| `11-history-filter-active.png` | History filtered to Active |
| `12-history-filter-failed.png` | History filtered to Failed |
| `13-history-filter-stopped.png` | History filtered to Stopped |
| `14-history-empty.png` | "No runs match." — a job that has never run |
| `15-history-winding-down.png` | A run asked to wind down: "stopping…" chip, wind-down button withdrawn |
| `16-log-drawer-live.png` | Live log tail (SSE) with Wind down + Stop now |
| `17-log-drawer-failed.png` | A finished failure, with stderr in the body |
| `18-log-drawer-timeout.png` | A timed-out run, including the SIGTERM line |
| `19-log-drawer-winding-down.png` | Already ringing: Stop now offered, Wind down withdrawn |
| `20-dialog-new-job-claude.png` | New job, Claude type, Advanced collapsed |
| `21-dialog-new-job-claude-advanced.png` | Advanced open: model, permissions, extra CLI args, budget guard |
| `22-dialog-new-job-command.png` | New job, Shell command type — one Command field, no extension advanced fields |
| `23-dialog-schedule-builder.png` | Four schedule rows in four different preset shapes at once |
| `24-dialog-validation-errors.png` | Server-side validation panel on save |
| `25-dialog-edit-job.png` | Edit an existing claude job, Advanced open and populated |
| `26-dialog-plan-burndown.png` | Plan burn-down, initial state with the job picker |
| `27-dialog-plan-burndown-preview.png` | A previewed plan: confidence, reasoning, slot table, confirm button (never clicked) |
| `28-projects-populated.png` | Three discovered repos, all pending / paused — none activated |
| `29-projects-ready-beads.png` | Ready-beads panel: id, priority, type and label pills from a real bd backlog |
| `30-projects-ready-all-fullpage.png` | All Ready panels open, showing the pending and paused row states *(fullpage)* |
| `31-dialog-burst.png` | Burst dialog with its budget presets; picker empty because nothing is activated |
| `32-sessions-populated.png` | Sessions tab: chip-grid cards for the planted fixture transcripts |
| `33-sessions-conversation.png` | An open conversation: per-tool turns (Edit diff, TodoWrite before/after, Grep counts) expanded |
| `34-settings-seeded.png` | Settings with demo values: usage monitor, budget & reserve, task sources |
| `35-settings-fullpage.png` | Every settings section in one image, Danger zone expanded *(fullpage)* |
| `36-settings-danger-zone.png` | Danger zone expanded — cleanup and uninstall confirmations |
| `37-settings-saved.png` | The "saved ✓" acknowledgement |
| `38-usage-compact-chip.png` | usageShow: compact — a header chip instead of the meter banner |
| `39-keep-awake-menu.png` | The ☕ keep-awake popover, all seven modes |

### Global pause states

| File | State |
|---|---|
| `40-pause-hold.png` | Hold: scheduled fires dropped, manual runs still allowed |
| `41-pause-soft.png` | Soft drain: nothing new starts, live work winds down |
| `42-pause-hard.png` | Hard stop: everything in flight killed immediately (red banner) |

